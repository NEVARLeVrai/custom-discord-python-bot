# Cogs for automatic chat detection and error handling
# These cogs contain:
# - Listeners on_message, on_mention, etc. (automatic detections)
# - Error handlers (ErrorHandler)
# They are separated from prefixed commands for better organization